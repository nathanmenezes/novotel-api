services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=novotel'
      - 'MYSQL_PASSWORD=1234'
      - 'MYSQL_ROOT_PASSWORD=root'
      - 'MYSQL_USER=nathan'
    ports:
      - '3306'
